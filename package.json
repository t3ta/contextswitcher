{
  "name": "contextswitcher",
  "version": "0.1.0",
  "type": "module",
  "description": "A lightweight MCP gateway for managing multiple AI agent contexts across projects and environments.",
  "main": "src/cli.ts",
  "bin": {
    "contextswitcher": "dist/cli.js"
  },
  "scripts": {
    "dev": "bun src/cli.ts",
    "build": "bun build src/cli.ts --outfile dist/cli.js --target node",
    "test": "vitest",
    "start": "node dist/cli.js"
  },
  "keywords": [
    "mcp",
    "ai",
    "gateway",
    "context",
    "cli",
    "typescript"
  ],
  "author": "t3ta",
  "license": "MIT",
  "devDependencies": {
    "vitest": "^1.0.0",
    "@types/node": "^22.14.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.9.0"
  }
}
